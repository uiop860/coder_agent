{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "cargo": {
        "args": [
          "build",
          "--bin=coder_agent",
          "--manifest-path",
          "${workspaceFolder}/Cargo.toml"
        ]
      },
      "envFile": "${workspaceFolder}/.env",
      "args": [],
      "cwd": "${workspaceFolder}",
      "terminal": "external"
    }
  ]
}
